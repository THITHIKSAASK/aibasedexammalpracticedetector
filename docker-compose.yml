version: '3'

services:
  app:
    image: node:14
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    ports:
      - '3000:3000'
    command: 'npm start'

  db:
    image: nouchka/sqlite3
    volumes:
      - ./data:/data
    command: sqlite3 /data/database.sqlite